apiVersion: v1
kind: Service
metadata:
    name: "deltron9-e2fd"
    labels:
        app: "deltron9-e2fd"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "deltron9-e2fd"