# Dockerfile Playground
Different base images with different subsequent installation scenarios, a repository to hold useful linux commands which I tend to forget!

Examples include;
- installing .NET Core X.X via packages.
- installing .NET Core X.X binaries.